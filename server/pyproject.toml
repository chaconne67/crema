[project]
name = "crema-server"
version = "0.1.0"
description = "crema-agent.site: homepage, Google sign-in and app accounts for Crema"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2,<5.3",
    "django-allauth[socialaccount]>=65.18,<66",
    "gunicorn>=23,<24",
    "psycopg[binary]>=3.2,<4",
]

[tool.uv]
package = false
